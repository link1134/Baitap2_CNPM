package view;

import controller.BoardController;
import controller.StartMenuController;

public class Game {
	StartMenu sm = new StartMenu();
	StartMenuController smc = new StartMenuController(sm);
	}
